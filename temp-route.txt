Add this to the TOP of your routes/web.php file (after the use statements):

// TEMPORARY LOGIN ROUTE - REMOVE AFTER UPDATE
Route::get('/temp-login', function () {
    return view('temp-login');
});

Then access your site at: /temp-login

This bypasses all the normal login page logic and gives you a clean form with just username and password fields.